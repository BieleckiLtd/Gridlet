## Summary

Describe the problem and the change.

## Validation

- [ ] Tests added or updated where appropriate
- [ ] `dotnet test --configuration Release` passes
- [ ] Documentation updated for user-visible behavior

## Release impact

Note breaking changes, migration steps, or package/version implications. Normal
feature pull requests target `dev`; only the release promotion targets `main`.
