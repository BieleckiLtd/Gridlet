<!-- The `note` field of the describe_gridlet_deployment result. The host may not have supplied an
     address for the installation, and without one the model reaches for the placeholder URLs in
     the guide and hands people links that resolve to nothing. -->
## with-installation
Counts are null when the host replaced Gridlet's default store. Build every URL you show from the installation facts above.

## without-installation
Counts are null when the host replaced Gridlet's default store. This turn was given no address for the installation, so do not state a URL for it.
