<!-- The topic list in the parameter section is generated from the file names in Guide/, so adding
     a topic file is enough; there is nothing to update here. -->
Read Gridlet's own documentation for one topic. Use it before answering any question about how Gridlet works — editing or deleting rows, creating or deleting database objects, publishing an API endpoint, parameters, paging and scale, the interface, security, the table designer, or configuration — so the answer matches the product instead of general knowledge. A question that starts "how do I..." is almost always answered here, and often by a feature of the interface rather than by SQL.

## topic
Topic to read: {topics}. Omit to list the topics.
