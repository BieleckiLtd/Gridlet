List tables, views, stored procedures, functions, and triggers in the selected database, optionally filtered by schema, name, or object type.

## schema
Optional database schema to restrict the results to.

## nameContains
Optional case-insensitive text that the object name must contain.

## objectType
Optional object type: Table, View, StoredProcedure, ScalarFunction, TableValuedFunction, or Trigger.
