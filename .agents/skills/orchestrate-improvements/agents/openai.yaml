interface:
  display_name: "Orchestrate Improvements"
  short_description: "Isolate, review, and integrate parallel improvements"
  default_prompt: "Use $orchestrate-improvements to audit this codebase, implement strong fixes in isolated worktrees without committing, review and integrate them, clean the worktrees, and summarize the results."
